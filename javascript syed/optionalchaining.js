let student={
    name:"uday",
    age:20,
    gender:"male",
    hobbies:{
        hero:"power"

    }
}
console.log(student)
console.log(student.name)
console.log(student.favhobbies.movie)





//& Short Circuit Operator(&& or ||) -->
//& used instead of if statement, i we use && -- it is true means it return next argument or false means same argument will be executed  || if false means next argument will be executed


//& Debouncing --> calling the API after some time using setTimeout

//& Throttling --> calling the API again and again using setInterval(dynamic search option)